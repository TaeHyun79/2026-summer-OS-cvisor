# IDE / build dirs
.idea/
cmake-build-debug/

# build artifacts (binaries have no extension; keep sources only)
*.o
/cvisor
tests/*
!tests/*.c
!tests/*.s
ch4/*
!ch4/*.c
!ch4/*.s
ch6/*
!ch6/*.c
!ch6/*.s

# machine-specific local VM config (kept out of the repo on purpose)
vm/
